import { DisplayCarousel } from "./hero-images";

export const HeroSection = () => {
  return (
    <section className="h-[60vh] w-full bg-stone-600">
      <DisplayCarousel />
    </section>
  );
};
